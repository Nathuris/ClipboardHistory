# CLAUDE.md

## 项目简介

**ClipboardHistory** — 一款 macOS 历史剪贴板管理软件。自动记录用户复制的文字和图片，支持搜索、置顶、删除，可设定保留天数（1/3/5天），黑名单 App 内容需 Touch ID 验证后查看。

- **用户画像**：不懂代码的小白用户
- **开发原则**：稳步推进，每阶段验收后再进入下一阶段，不跳跃开发

---

## 标准文档索引

每次开发前，必须先查阅对应的标准文档。所有文档位于 `docs/` 文件夹：

| 文档 | 路径 | 何时查阅 |
|------|------|----------|
| 需求规格 | [`docs/requirements.md`](docs/requirements.md) | 不确定功能细节时 |
| 技术规格 | [`docs/tech-spec.md`](docs/tech-spec.md) | 技术选型、依赖版本、存储路径有疑问时 |
| UI 设计规范 | [`docs/design-spec.md`](docs/design-spec.md) | 写 UI 代码时（配色、字体、尺寸、图标） |
| 架构设计 | [`docs/architecture.md`](docs/architecture.md) | 不确定模块归属、数据流、DB Schema 时 |
| 执行步骤 | [`docs/implementation-plan.md`](docs/implementation-plan.md) | **每次开发前必读**（确认当前阶段和任务） |

---

## 开发日志

日志文件位于 `dev-logs/` 文件夹，按日期命名：`YYYY-MM-DD.md`。

### 日志格式

```markdown
# 开发日志 — YYYY-MM-DD

## 当前阶段
Phase X — 阶段名称

## 今日完成
- [x] 事项 1
- [x] 事项 2

## 待办事项
- [ ] 事项 A
- [ ] 事项 B

## 遇到的问题
- 问题及解决方案

## 备注
- 其他需记录的信息
```

### 日志规则

1. **每次开发会话结束时**，必须更新当日日志
2. 如果当日日志已存在，追加到已有内容中（不要覆盖）
3. 将完成的事项从「待办事项」移到「今日完成」
4. 如有新出现的待办事项，添加到「待办事项」中
5. 如遇问题及解决方案，记录在「遇到的问题」中

---

## 工作约定

### 开发流程

1. **开始开发前**
   - 阅读 [`docs/implementation-plan.md`](docs/implementation-plan.md) 确认当前 Phase
   - 阅读当前 Phase 的任务清单和验收条件
   - 阅读相关标准文档（需求、设计、架构）

2. **开发过程中**
   - 严格按照当前 Phase 的子任务逐项完成
   - 不跳到其他 Phase 做提前开发
   - 遇到不确定的设计决策，查阅 `docs/` 中的标准文档
   - 如标准文档未覆盖，向用户确认后，先更新标准文档再开发

3. **开发结束后**
   - 更新 [`dev-logs/YYYY-MM-DD.md`](dev-logs/) 当日日志
   - 更新 [`docs/implementation-plan.md`](docs/implementation-plan.md) 中的任务状态
   - 如当前 Phase 全部完成，标记为 ✅ 并更新总体进度

4. **验收**
   - 每个 Phase 所有子任务完成后，对照验收条件逐项检查
   - 全部验收通过后，再进入下一 Phase

### 沟通约定

- **用户是小白的含义**：不需要用户懂任何技术概念。解释时用通俗比喻，报进度时给可感知的成果
- **报进度格式**：用简单的勾选列表，让用户一眼看懂什么完成了、什么在进行
- **技术术语**：首次出现时必须解释（如"数据库→就像一个 Excel 表格"）

### 代码约定

- 所有注释使用中文
- 变量和函数名使用英文（驼峰命名）
- UI 代码严格遵循 [`docs/design-spec.md`](docs/design-spec.md) 的规范值
- 配色使用设计规范中定义的 Hex 值，不随意使用其他颜色

---

## 用户偏好

- 软件运行方式：**菜单栏小图标**
- 全局快捷键：**需要**（默认 Cmd+Shift+V）
- 开机自启：**需要**
- 隐私策略：**黑名单 + Touch ID 验证**
- 保留天数：**1/3/5 天可选**
- UI 风格：**简洁直观，淡蓝色主色调**

---

## 项目环境

- **开发工具**：Xcode 16+（macOS 原生）
- **最低系统**：macOS 14 (Sonoma)
- **语言**：Swift 5.9+
- **依赖管理**：Swift Package Manager
